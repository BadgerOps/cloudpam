apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudpam-server.fullname" . }}
  labels:
    {{- include "cloudpam-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudpam-server.selectorLabels" . | nindent 4 }}
